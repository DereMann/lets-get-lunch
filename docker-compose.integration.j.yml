version: '3.1'
 
services:
  mongo:
    image: mongo
    ports:
    - "27017:27017"
    volumes:
    - data-volume:/data/db
  web_api:
    volumes:
    - ./lets-get-lunch-api/:/usr/src/app
    - /usr/src/app/node_modules
    build: ./lets-get-lunch-api/
    ports:
    - "8080:8080"
    - 9229:9229
    command: bash -c "gulp && npm run api-test"
    depends_on:
    - mongo

  web_app:
    volumes:
    - ./lets-get-lunch/:/usr/src/app
    - /usr/src/app/node_modules
    image: "letsgetlunchapp:B${BUILD_NUMBER}"
    ports:
    - "4200:4200"
    command: bash -c "npm install && ng test --watch=false"
    depends_on:
    - web_api
    
volumes:
    data-volume:
